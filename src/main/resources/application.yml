server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/typing_game?useSSL=false&serverTimezone=Asia/Tokyo
    username: root
    password: Takahumi7
    driver-class-name: com.mysql.cj.jdbc.Driver

  mybatis:
    mapper-locations: classpath:mapper/WordMapper.xml
    type-aliases-package: com.example.entity

# JWTやセキュリティ設定を後で追加可能
