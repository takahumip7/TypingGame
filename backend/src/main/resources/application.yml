server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/typing?useSSL=false&serverTimezone=Asia/Tokyo
    username: root
    password: Takahumi7
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# JWTやセキュリティ設定を後で追加可能
